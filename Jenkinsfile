pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Define the Git repository URL
                script {
                    git 'https://github.com/your-username/your-repository.git'
                }
            }
        }
    }
}
