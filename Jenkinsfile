pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Define the Git repository URL
                script {
                    git 'https://github.com/santoshbd67/scripts.git'
                }
            }
        }
    }
}
